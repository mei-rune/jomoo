#include "StdAfx.h"
#include "FileManager.h"
#include "BOE/exception.hpp"

_greengown_begin

FileManager::FileManager( Instance_IM& instance )
: instance_( instance )
{
	toString_ = BT_TEXT("DownLoad文件管理器");
}

Data_Block_Ptr FileManager::readFile( const tstring& url, size_t begin, size_t end )
{
}

const tstring& FileManager::toString() const
{
	return toString_;
}

_greengown_end
