
#ifndef Message_Buffer_H
#define Message_Buffer_H

#include "config.h"

#if !defined (BOE_LACKS_PRAGMA_ONCE)
# pragma once
#endif /* BOE_LACKS_PRAGMA_ONCE */

// Include files
#include "config_IM.h"
#include "MsgConnection.H"


_im_begin

class Simple_Data_Block : public Data_Block
{
public:
	Simple_Data_Block( Data_Block_Ptr ptr ,size_t begin , size_t end );
	Simple_Data_Block( Data_Block_Ptr ptr ,size_t begin );

	const char* rd_ptr( ) const;
	size_t length( ) const;
private:
	Data_Block_Ptr ptr_;
	size_t begin_;
	size_t end_;
};
_im_end

#endif // Message_Buffer_H

